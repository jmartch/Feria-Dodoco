services:
  mysql:
    image: mysql:8
    ports:
      - "3308:3306"
    environment:
      MYSQL_ROOT_PASSWORD: dodoco
      MYSQL_DATABASE: dodoco
    volumes:
      - dodoco_mysql:/var/lib/mysql

volumes:
  dodoco_mysql:
